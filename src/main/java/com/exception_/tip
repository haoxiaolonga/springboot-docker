###
   1）filter中出现异常时，全局异常处理器中是无法捕获的。
   2）全局异常处理器只能捕获controller中的异常。
   3）filter中出现的异常只能单独处理。

   所以可以通过：
   第一步：在filter中注入HandlerExceptionResolver
   第二步：然后在catch中抛出 异常调用 --> resolveException()方法;
   第三步：之后在@RestControllerAdvice 或者@ControllerAdvice 修饰的全局异常中定义需要处理的异常类型，例如JwtException等